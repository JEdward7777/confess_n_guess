Figure out how you send the server to a new screen.

Make it so that the server can continue on after showing the users.

Fix the control c thing which is bypassing the state of the game being saved.

Make it so that if you refresh your screen, you don't lose your location.

----

Fix it so that when you put your name it it doesn't send you to the host screen with the count down.

Make it so that when you answer the question on screen 9 that it then goes to a screen saying thank you until the next phase.

Make it so that the question which shows on teh answer teh question page, screen 9 doesn't show <br> in the question.

Make it so that if if you are in the wrong game state you can get back to the main screen.

create a function that I can send to the server to kill it.

Make the server show a countdown

Make it so that if a gave has no activity for enough time that it gets deleted.

Make it so that when the countdown in h2 reaches zero that it sends an event to the server to go onto the next thing.

Make it so that if you create a username that already exists that it fusses.
|
Actually make it so that you replace that user so that it is possible to get back into the game if you fall out.

Make it so that you can continue the server without clicking a button on the server because it needs to be just a screen.

Make it so that if you put in you name and emogi it doesn't go to screen 2 because that is a screen for the server.