{
  "name": "fibbage_knockoff",
  "version": "1.0.0",
  "description": "A knockoff of fibbage LLM style.",
  "main": "index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build_server": "tsc",
    "build_client": "(cd fibbage_knockoff_client && npm run build)",
    "build": "npm run build_client && npm run build_server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Lansford",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.4",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.21"
  }
}
